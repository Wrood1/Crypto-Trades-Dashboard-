import { Routes } from '@angular/router';
import { TradeTableComponent } from './components/trade-table/trade-table.component';

export const routes: Routes = [
  { path: '', component: TradeTableComponent }
];
